name: Build FreeRTOS ARM

on: [push, pull_request]

jobs:
  build:
    name: GCC ${{ matrix.gcc }} ${{ matrix.mode }}
    runs-on: ubuntu-latest
    container:
      image: kubasejdak/arm-none-eabi-gcc:${{ matrix.gcc }}
    strategy:
      matrix:
        gcc: [8]
        mode: [Debug, Release]

    steps:
    - uses: actions/checkout@v1
      with:
        submodules: true

    - name: Configure conan
      run: |
        cp /root/.conan/settings.yml `conan config home`
        mkdir -p `conan config home`/profiles
        cp /root/.conan/profiles/default `conan config home`/profiles

    - name: Build application
      run: |
        mkdir build
        cd build

        conan install .. --build fmt
        cmake .. -DPLATFORM=freertos-arm -DCMAKE_BUILD_TYPE=${{ matrix.mode }}
        make
