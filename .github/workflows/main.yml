name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: gcc:9.2.0

    steps:
    - uses: actions/checkout@v1
      with:
        submodules: true
    - name: Action test
      run: |
        echo "Some test string"
        ls -l
        gcc -v
        g++ -v
